#include<stdio.h>
#include<stdlib.h>

int main(){
    char *str, *s1,*s2;
    printf("Enter the string");
    scanf("%d",&str);

    int ind;
    printf("Enter the index you want to replace= ");
    scanf("%d",&ind);

    for(int i=0;i<len(str);i++)
    {
        if(str='\0')
            return 0;
            
    }
}